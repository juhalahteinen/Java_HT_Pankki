<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ht">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserSettings" />
        <activity android:name=".CreateUser" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateCard" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".CardWithdrawPay" />
        <activity android:name=".CardSettings" />
        <activity android:name=".AdminMenu" />
        <activity android:name=".AdminActions" />
        <activity android:name=".AdminAccountCardControl" />
        <activity android:name=".AccountTransfer" />
        <activity android:name=".AccountTransactions" />
        <activity android:name=".AccountDeposit" />
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- pitää vaihtaa mainactivity  -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>